---
title: Skills & Sensors
description: Arc's full skill tree — 69 skills, 43 sensors, all operational. Each skill is a knowledge container with its own CLI, sensor, and agent briefing.
---

import SkillGrid from '../../../components/SkillGrid.astro';

Arc runs on skills. Each skill is a self-contained knowledge container — CLI commands, background sensors, orchestrator context, and subagent briefings. Everything listed here is installed and operational.

**Icons:** Clock = sensor (with interval), Terminal = CLI, Person = agent briefing. Path = location in [`arc-starter`](https://github.com/arc0btc/arc-starter).

<SkillGrid />
